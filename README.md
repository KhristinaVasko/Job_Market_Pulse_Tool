# Job Market Pulse

A full-stack analytics tool that tracks job posting trends (skills, salaries, 
demand) using the Adzuna API.

## Stack
- Backend: Flask + SQLAlchemy + PostgreSQL
- Frontend: React + D3.js/Plotly
- Ingestion: scheduled Python scripts pulling from Adzuna API

## Setup
(coming soon)